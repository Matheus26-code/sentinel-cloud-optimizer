# ============================================================
# Perfil de desenvolvimento local (docker-compose)
# Este arquivo NÃO deve ser commitado — está no .gitignore
# ============================================================

spring.datasource.url=jdbc:postgresql://localhost:5433/sentinel_cloud
spring.datasource.username=admin
spring.datasource.password=sentinel_password
